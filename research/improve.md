For model armour: it won't work for text rendered as an image with no text layer; instructions in PDF metadata; a payload split so each half is benign; and content that is persuasive-but-false rather than instruction-shaped


